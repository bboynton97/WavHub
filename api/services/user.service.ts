// import {UserModel} from "../models";
// import {User} from "types";

export class UserService {
    constructor() {
    }

    // async getByCustomerId(customerId: string): Promise< User> {
    //     return await UserModel.findOne({squareCustomerId: customerId}) as User
    // }
}